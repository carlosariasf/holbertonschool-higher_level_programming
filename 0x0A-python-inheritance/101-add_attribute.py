#!/usr/bin/python3
""" """
def add_attribute(nclass, nameu, lastnameu):
    """ """

    setattr(nclass, name, nameu)
    setattr(nclass, lastname, lastnameu)

    raise TypeError("can't add new attribute")"""
